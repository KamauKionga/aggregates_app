{
  "name": "aggregates_app_functions",
  "version": "1.0.0",
  "description": "Cloud Functions for Aggregates App",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "eslint . --ext .ts",
    "serve": "firebase emulators:start --only functions,firestore",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "16"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^3.20.0"
  },
  "devDependencies": {
    "typescript": "^4.8.4",
    "eslint": "^8.10.0",
    "@types/node": "^16.11.7"
  }
}